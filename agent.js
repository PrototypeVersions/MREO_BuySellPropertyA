(()=>{"use strict";const $=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
 async function start(){const status=$("agent-status");try{if(!MreoIdentity.connected())throw Error("Deploy Connected Mode to activate the agent console.");await MreoIdentity.init();if(!await MreoIdentity.mountUserButton($("agent-user"))){status.innerHTML=`<h3>MREO Agent sign-in required</h3><button class="primary-button button-blue" id="agent-sign-in">Sign in</button>`;$("agent-sign-in").onclick=()=>MreoIdentity.openSignIn();return;}const {transactions}=await MreoIdentity.request("/api/v1/agent/transactions");$("agent-total").textContent=transactions.length;$("agent-actions").textContent=transactions.reduce((n,t)=>n+Number(t.actionCount||0),0);$("agent-closing").textContent=transactions.filter(t=>t.status==="closing").length;$("agent-list").innerHTML=transactions.length?transactions.map(t=>`<a class="transaction-row" href="coordination.html?transaction=${encodeURIComponent(t.id)}"><div><h3>${esc(t.title)}</h3><p>${esc(t.status)} · ${esc(t.kind)}</p></div><div class="transaction-meta"><span class="attention-chip">${Number(t.actionCount||0)} action${Number(t.actionCount||0)===1?"":"s"}</span></div></a>`).join(""):`<div class="empty-card"><h3>No active transactions</h3></div>`;status.hidden=true;$("agent-app").hidden=false;}catch(e){status.innerHTML=`<h3>Agent console unavailable</h3><p>${esc(e.message)}</p>`;}}
 start();})();
